[Network]
Scheme = http
Domain = www.time.gov
Query = Eastern/d/-5
Action = timezone.cgi
Url = ${Scheme}://${Domain}/${Action}?${Query}
LogFile = ${Network|Scheme}.xml ; note that this is an external secion lookup, this will work across files.